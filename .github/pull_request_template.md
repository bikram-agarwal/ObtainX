## Target branch

This repository keeps `main` plus at most one working branch at a time.

- If a working branch other than `main` exists, target it.
- Target `main` only when it is the only branch.

If you opened this against the wrong branch, a bot will retarget it automatically.

## What this changes

<!-- Describe the change and why it is needed. -->

## Testing

<!-- How did you verify it? -->
